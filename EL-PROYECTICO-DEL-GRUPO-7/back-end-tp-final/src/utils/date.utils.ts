/* import { DateTime as Luxon } from 'luxon';
//TODO: INSTALAR LUXON

/**
 * @description funcion que retorna la fecha actual
 * @returns Fecha actual
 
export const timeToStringFormat = (dateFromSQL: string): string => {
    return Luxon.fromSQL(dateFromSQL).toFormat('HH:mm:ss');
};
export const dayToStringFormat = (dateFromSQL: string): string => {
    return Luxon.fromSQL(dateFromSQL).toFormat('yyyy-MM-dd');
};
export const dateNow = () => {
    return Luxon.now();
};
export const last24Hours = (date: Luxon) => {
    return date.minus({ hours: 24 });
};

export const nameFolderDate = (date: Date = new Date()) => {
    let temp = date;
    if (typeof temp === 'string') {
        temp = new Date(temp);
    }
    return temp.toISOString().split('T')[0].split('-').join('');
}; */